{
  "scripts": {
    "generate": "fantasticon icons -o . --name Icont --prefix icont --tag '' --normalize true --asset-types scss && node-sass ./Icont.scss > ./font.css --output-style compressed"
  }
}
